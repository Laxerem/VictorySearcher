Compact uppercase label. Neutral outline by default; a neon `accent` adds the category color and glow.

```jsx
<Badge>SEO</Badge>
<Badge accent="orange">Реклама</Badge>
<Badge accent="cyan">Лидогенерация</Badge>
```

Use accents only for service-category tags — keep them rare so the glow stays special.
