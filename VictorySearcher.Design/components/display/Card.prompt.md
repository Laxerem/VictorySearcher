Base dark surface card: subtle top-lit gradient, hairline border, deep shadow, sharp corners. Compose content inside it.

```jsx
<Card interactive>
  <h3>...</h3>
  <p>...</p>
</Card>
```

Pass `interactive` for clickable cards (hover lift + lighter gradient). For the branded service block with a corner "›", use `ServiceCard` instead.
