Dark text field. `underline` is the CTA phone field (bottom rule only); `box` is a bordered field for denser forms. Focus brightens the rule/border to white.

```jsx
<Input variant="underline" placeholder="Укажите номер телефона" />
<Input variant="box" placeholder="Ваш e-mail" />
```
